{
  "name": "wabbajack-nexus-index",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "See which Wabbajack modlists include a Nexus mod, right on its Nexus Mods page. The extension also adds direct Nexus Mods links to Wabbajack archive lists. Unofficial extensions for Chrome and Firefox.",
  "type": "module",
  "scripts": {
    "test:python": "python -m unittest discover -s tests/python -v",
    "test:js": "node --test tests/js/*.test.mjs",
    "test": "npm run test:python && npm run test:js",
    "build": "node scripts/build-extension.mjs && python scripts/package_extensions.py",
    "verify": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
